This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, install the needed dependencies and libraries

```bash
npm install
# or
pnpm install
# or
yarn install
# or bun install
```

Then run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.


## Deploy on Vercel

You can view the deployed link for this project at: [Project Link](https://expert-listing-demo.vercel.app/)
